"""
Capture the sketch via a simple HTTP endpoint that accepts base64 image upload
"""
import sys, base64, os

# Alternative: use the canvas screenshot approach
# For now, let's create the preprocessing pipeline and test with a captured screenshot
print("Pipeline script ready - need image input")
